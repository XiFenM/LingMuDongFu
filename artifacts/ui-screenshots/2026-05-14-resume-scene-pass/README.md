# UI screenshots archive — resume/scene asset pass

- `screenshots/`: Playwright full-page screenshots for `/resume`, `/404`, and `/`.
- `asset-previews/`: contact sheet for generated resume/scene assets.
- `compare/`: compare_mockup outputs for resume and 404 pages.

See `manifest.json` for move details.
